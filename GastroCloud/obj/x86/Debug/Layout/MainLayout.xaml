<Page
    x:Class="GastroCloud.Views.MainLayout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GastroCloud.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--Recursos-->
    <Page.Resources>
        <SolidColorBrush x:Key="NavButtonPressedBackgroundBrush" Color="White" Opacity="0.3" />
        <SolidColorBrush x:Key="NavButtonCheckedBackgroundBrush" Color="White" Opacity="0.2" />
        <SolidColorBrush x:Key="NavButtonHoverBackgroundBrush" Color="White" Opacity="0.1" />

        <Style x:Key="HamburguerRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background" Storyboard.TargetName="BackgroundGrid">
                                                <DiscreteObjectKeyFrame KeyTime="10" Value="{StaticResource NavButtonHoverBackgroundBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background" Storyboard.TargetName="BackgroundGrid">
                                                <DiscreteObjectKeyFrame KeyTime="10" Value="{StaticResource NavButtonPressedBackgroundBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled" />
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background" Storyboard.TargetName="BackgroundGrid">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource NavButtonCheckedBackgroundBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked"/>
                                    <VisualState x:Name="Indeterminate"/>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="FocusStates">
                                    <VisualState x:Name="Focused"/>
                                    <VisualState x:Name="Unfocused"/>
                                    <VisualState x:Name="PointerFocused"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid Name="BackgroundGrid" Background="Transparent" Margin="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="48"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock FontSize="16" Height="16" Text="{TemplateBinding Tag}" FontFamily="Segoe MDL2 Assets" Margin="0,16" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White"/>
                                <ContentPresenter x:Name="ContentPresenter"
                                                 FontSize="16"
                                                 AutomationProperties.AccessibilityView="Raw"
                                                 ContentTemplate="{TemplateBinding ContentTemplate}"
                                                 ContentTransitions="{TemplateBinding ContentTransitions}"
                                                 Content="{TemplateBinding Content}" Grid.Column="1"
                                                 HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                 Margin="{TemplateBinding Padding}"
                                                 TextWrapping="Wrap"
                                                 VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                 Foreground="White" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <!--Terminan Recursos-->


    <SplitView x:ConnectionId='1' x:Name="HeaderNav" CompactPaneLength="50" OpenPaneLength="300" IsPaneOpen="False"  DisplayMode="CompactInline">

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:ConnectionId='2' x:Name="VisualStateGroup"/>
        </VisualStateManager.VisualStateGroups>
        <!--Header nav-->

        <SplitView.Pane >

            <ScrollViewer>
                <StackPanel Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                    <RadioButton x:ConnectionId='3'                            Content="" Tag="" Style="{StaticResource HamburguerRadioButtonStyle}" />
                    <StackPanel Background="{ThemeResource SystemControlBackgroundAltMediumLowBrush}" Margin="0,0,0,20" Padding="0,20">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="72"  Foreground="White" TextAlignment="Center"></TextBlock>
                        <TextBlock Text="NOMBRE EMPRESA" TextAlignment="Center" Style="{StaticResource BodyTextBlockStyle}" Foreground="White"></TextBlock>
                    </StackPanel>

                    <RadioButton Content="Home" Tag="" Style="{StaticResource HamburguerRadioButtonStyle}" />
                    <RadioButton  Content="Grupos" Tag="" Style="{StaticResource HamburguerRadioButtonStyle}" />
                    <RadioButton  Content="Almacenes" Tag="" Style="{StaticResource HamburguerRadioButtonStyle}" />
                    <RadioButton  Content="Insumos" Tag="" Style="{StaticResource HamburguerRadioButtonStyle}" />
                    <RadioButton  Content="Proveedores " Tag="" Style="{StaticResource HamburguerRadioButtonStyle}" />
                    <RadioButton  Content="Presentaciones" Tag="" Style="{StaticResource HamburguerRadioButtonStyle}" />
                    <RadioButton  Content="Productos" Tag="" Style="{StaticResource HamburguerRadioButtonStyle}" />
                    <RadioButton  Content="Compras" Tag="" Style="{StaticResource HamburguerRadioButtonStyle}" />
                    <RadioButton  Content="Reportes" Tag="" Style="{StaticResource HamburguerRadioButtonStyle}" />
                </StackPanel>
            </ScrollViewer>
        </SplitView.Pane>
        <!--Termina Header nav-->


        <!--Contenido-->
        <SplitView.Content>

            <Frame x:ConnectionId='4' x:Name="contentFrame">
                <!--Ventana contruida a partir de otras-->
            </Frame>

        </SplitView.Content>
        <!--Termina Contenido-->
    </SplitView>



</Page>

